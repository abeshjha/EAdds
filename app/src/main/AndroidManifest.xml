<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="studio.androidapp.suj.e_adds">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Helper.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity.MainActivity" />
        <activity
            android:name=".Register.SignUpActivity"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Picture.Picture" />
        <activity android:name=".UserSettings.UserProfile" />
        <activity android:name=".Audio.AudioAdd" />
        <activity android:name=".Video.VideoAdd" />
        <activity android:name=".Picture.SinglePicture" />
        <activity android:name=".Audio.SingleAudio" />
        <activity android:name=".Video.SingleVideo" />
        <activity
            android:name=".MainActivity.dashboard"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UserSettings.AccountSetting"></activity>
    </application>

</manifest>